# Secrets — ne jamais commiter
config.php

# Cache TMDB généré automatiquement à l'exécution
.movies_meta.json

# Fichiers système
.DS_Store
Thumbs.db

# Fichiers de test créés pendant le développement
phpver.php
hashtest.php
tmdbtest.php

# Anciennes versions
*.old

# Cache scan + posters téléchargés (générés à l'exécution)
.scan_cache.json
.posters/

# Environnement de diagnostic Docker (diag/docker-compose.yml) : fichier de
# test creux + config générée dans la racine du dépôt via le bind mount
testfile.mkv
/diag.php

# Films de test réels utilisés pour reproduire les bugs de taille >2/4Go
/Legends.2026.S01.MULTi.1080p.WEB.x264-FW/
